#include <bits/stdc++.h>

using namespace std;

void run_case(){
    int n, k; cin >> n >> k ;
    vector<int> v(n);
    for(int i=0;i<n;i++){
        cin >> v[i];
        
    }
}

int main(){
    int t; cin >> t;
    while(t--){
        run_case();
    }
}