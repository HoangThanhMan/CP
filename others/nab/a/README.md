# NAB — bài A

- **Nguồn:** <dán link đề bài>
- **Độ khó:** —
- **Tags:** `scratch`
- **Ngày giải:** 2026-07-29
- **Kết quả:** CHƯA GIẢI — cả hai file đều là khung dở dang

> `solution.cpp` chỉ có `main()` rỗng, `solution.py` dừng giữa chừng ở `if("99" in S):`. Giữ lại để không mất dấu vết; xoá được nếu không cần.

## Đề bài (tóm tắt)

Chưa xác định được đề bài từ code còn lại. Có vẻ liên quan tới việc xét chuỗi chữ số và tìm mẫu "99" trong đó.

## Ý tưởng

Chưa có.

## Độ phức tạp

- Thời gian: `O(?)`
- Bộ nhớ: `O(?)`
